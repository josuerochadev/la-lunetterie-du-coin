import { SERVICES_DATA } from '@/data/services';

export const SERVICE_COUNT = SERVICES_DATA.length;

// Scroll budget (normalised 0–1)
export const SERVICES_START = 0.06;
export const SERVICES_END = 0.94;
